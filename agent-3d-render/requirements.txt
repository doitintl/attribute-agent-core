# 3D Rendering SaaS Agent Dependencies
# For Amazon Bedrock AgentCore Runtime Instances (GPU)

# AgentCore Runtime SDK - handles the /invocations + /ping contract for us
bedrock-agentcore>=0.1.0

# Strands Agents SDK
strands-agents>=0.1.0
strands-agents-tools>=0.1.0

# AWS SDK
boto3>=1.34.0

# OpenTelemetry for AgentCore observability
opentelemetry-distro>=0.44b0
opentelemetry-exporter-otlp>=1.23.0
